extends layout

block content
    form(method='GET' action="/search")
        .searchBar
            input.u-full-width(type='text' name="ticker" placeholder='ticker')
            button.button-primary(type='submit') 🕵️‍♂️