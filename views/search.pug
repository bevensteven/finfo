extends layout

block content
    form(method='GET' action="/search")
        .wrap.search
            input(type='text' name="ticker" placeholder='ticker')
            button.searchButton(type='submit') 🕵️‍♂️